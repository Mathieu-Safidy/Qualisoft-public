import { Identification } from './identification';

describe('Identification', () => {
  it('should create an instance', () => {
    const directive = new Identification();
    expect(directive).toBeTruthy();
  });
});
